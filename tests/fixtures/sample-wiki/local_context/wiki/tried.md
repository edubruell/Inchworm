# tried — tried X, failed because Y, don't retry unless Z

## [2026-08-17] Tried a keyed store → dropped. Failed because compaction ran during reads. Don't retry unless the record count outgrows one file.
